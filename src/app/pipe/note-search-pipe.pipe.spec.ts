import { NoteSearchPipePipe } from './note-search-pipe.pipe';

describe('NoteSearchPipePipe', () => {
  it('create an instance', () => {
    const pipe = new NoteSearchPipePipe();
    expect(pipe).toBeTruthy();
  });
});
